import React from 'react';

import { Container, ContentText } from './styles';

const Layout: React.FC = () => {
  return (
    <Container>
      <ContentText>Layout</ContentText>
    </Container>
  );
}

export default Layout;
